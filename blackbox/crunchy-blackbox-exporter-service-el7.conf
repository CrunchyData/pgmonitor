# This file must have a .conf extension to override the default service and activate the crunchy specific configuration
# Note this file must live in /etc/systemd/system/blackbox_exporter.service.d/ to work
#
# Override package service config with our own as needed. ExecStart must be reset.

[Service]
PermissionsStartOnly=true
User=ccp_monitoring
EnvironmentFile=/etc/sysconfig/blackbox_exporter
ExecStart=
ExecStart=/usr/bin/blackbox_exporter $OPT
ExecReload=/usr/bin/kill -HUP $MAINPID
Restart=always

