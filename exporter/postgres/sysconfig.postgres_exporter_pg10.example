# Copy this file to a name that matches the EnvironmentFile entry in the service file (Default: /etc/sysconfig/postgres_exporter)

# For WEB_LISTEN_ADDRESS, change 'localhost' to the network IP assigned to this system if it will be queried non-locally
# For QUERY_PATH, the path below is the default place we recommend putting the custom queries file
# For DATA_SOURCE_NAME, set the database that the exporter will connect to. Default is `postgres`

WEB_LISTEN_ADDRESS="--web.listen-address=localhost:9187"
QUERY_PATH="--extend.query-path=/var/lib/ccp_monitoring/postgres_exporter/10/queries.yml"
DATA_SOURCE_NAME="postgresql://ccp_monitoring@localhost:5432/postgres?sslmode=disable"

