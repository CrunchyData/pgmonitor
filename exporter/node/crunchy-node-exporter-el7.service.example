# Copy, rename this file, and uncomment line(s) below to activate the crunchy specific configuration (recommend just removing .example suffix from copy)
# Note this file must live in /etc/systemd/system/node_exporter.service.d/ to work
#
# Override package service config with our own as needed. ExecStart must be reset.

#[Service]
#PermissionsStartOnly=true
#User=ccp_monitoring
#EnvironmentFile=/etc/sysconfig/node_exporter
#ExecStart=
#ExecStart=/usr/bin/node_exporter ${OPTIONS}
#ExecReload=/usr/bin/kill -HUP $MAINPID
#Restart=always
